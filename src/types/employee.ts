export type EmployeeUI = {
  id: string;
  nom: string;
  prenom: string;
  email: string;
  phone: string;
  matricule: string;
  permis: string;
  hireDate: string;
  category: string;
  permanent: boolean;
  department: string;
};